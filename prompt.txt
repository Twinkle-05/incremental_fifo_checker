Complete the missing RTL for `generator` and `pattern_checker`.

`fifo_top` and `fifo_sync` are complete and must not be modified.

Requirements:
- Generate an 8-bit LFSR-based pseudo-random byte pattern.
- Use the same LFSR polynomial in generator and checker.
- Pack 8 generated bytes into one 64-bit FIFO word.
- Generator must write only when `en=1` and `almost_full=0`.
- If stalled, generator must hold LFSR state, byte position, and partial word.
- Checker must independently regenerate expected data using FIFO read timing only.
- No direct state/sync signal from generator to checker.
- `out=1` when FIFO output matches expected data, else `out=0`.
- Use synthesizable Verilog/SystemVerilog only.
- Preserve all existing module interfaces.